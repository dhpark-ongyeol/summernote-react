/**
 * @summernote/react — React bindings for @summernote/core.
 *
 * Phase 0 stub: proves the package builds with react/react-dom as peer deps and
 * externalized from the bundle. The real surface (controlled <SummernoteEditor>,
 * useSummernote hook, per-instance ThemeContext, uncontrolled editable ref) lands
 * in Phase 1/3.
 */

export const REACT_BINDINGS_VERSION = '0.0.0';
